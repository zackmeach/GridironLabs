# core

Cross-cutting concerns:

- `config.py` — AppPaths/AppConfig, env/.env ingestion, feature flags.
- `logging.py` — structured logging and rotation.
- `models.py` — domain summary types shared across layers.
- `errors.py` — common exception hierarchy.
