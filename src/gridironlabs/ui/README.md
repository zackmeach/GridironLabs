# ui

PySide6 desktop shell pieces:

- `app.py` — application bootstrapper for the desktop runtime.
- `main_window.py` — maximized window with persistent top nav and stacked pages.
- `widgets/` — reusable navigation bar, state banners, and placeholder state panels.
- `resources` (../resources) — dark theme QSS and future assets.
