 # resources/
 
 Static assets consumed by the UI.
 
- `styles/` contains QSS themes (dark-first) including the top navigation bar styling and placeholder states. Add icons/fonts here as they are introduced.
