 # tests/
 
 Placeholder for future unit and integration tests.
 
 Suggested focus areas:
 - Data ingestion/parquet roundtrips.
 - Service behaviors (search accuracy, summary retrieval).
 - UI smoke tests (e.g., widget creation with pytest-qt).
